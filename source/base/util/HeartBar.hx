package util;

import flixel.FlxSprite;
import flixel.group.FlxSpriteGroup.FlxTypedSpriteGroup;

// Para un futuro no muy lejano
class HeartBar extends FlxTypedSpriteGroup<FlxSprite>
{
	public function new(_x:Int = 0, _y:Int = 0)
	{
		super(_x, _y);
	}
}
